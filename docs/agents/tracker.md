# Tracker config (read by spec-to-issues, code-review; written once by setup)
# One tracker per repo. Changing modes later is a migration (see agents/README.md), not an edit.
# Skills READ this file; only setup / first-time detection WRITES it.

mode: local
issues_dir: docs/issues
labels: ready-for-agent, blocked
